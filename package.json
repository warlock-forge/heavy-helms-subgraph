{
  "name": "heavy-helmsheavy-helms-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ heavy-helmsheavy-helms-subgraph",
    "create-local": "graph create --node http://localhost:8020/ heavy-helmsheavy-helms-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ heavy-helmsheavy-helms-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 heavy-helmsheavy-helms-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.96.0",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
